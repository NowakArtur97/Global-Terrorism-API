#
# Spring Data Neo4j
#
spring:
  data:
    neo4j:
      password: ${GRAPHENEDB_BOLT_PASSWORD}
      uri: ${GRAPHENEDB_URL}
      username: ${GRAPHENEDB_BOLT_USER}
      encryption: true
#      password: ${NEO4J_PASSWORD}
#      uri: ${NEO4J_BOLT}
#      username: ${NEO4J_USER}